version: "2"
services:
  web:
    build: "web"
    volumes:
      - "..:/www"
      - "./web/logs:/var/log/web"
    ports:
      - "127.0.0.1:8080:80"
    links:
      - db
      - redis
  db:
    build: "mysql"
    volumes:
      - "./mysql/data:/var/lib/mysql"
    environment:
      MYSQL_ROOT_PASSWORD: "root"
      MYSQL_DATABASE: "app"
    ports:
      - "127.0.0.1:3306:3306"
  redis:
    build: "redis"
    ports:
      - "127.0.0.1:6379:6379"
